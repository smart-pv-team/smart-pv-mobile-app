import {StyleSheet} from 'react-native';
import {DeviceCard} from '../../AppStyles';

const styles = StyleSheet.create({
  container: DeviceCard.container,
  photo: DeviceCard.photo,
  title: DeviceCard.title,
  category: DeviceCard.category,
});

export default styles;
